# 3D-Printed-Forge-Catalog
Simple script that generates a structure of static HTML pages with a home. It allows to search inside a directory all the images files and make a simple catalog to share.

npm start
